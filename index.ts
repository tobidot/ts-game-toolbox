import { SignalSocket } from "./src/signals/SignalSocket";
import { ObservableSocket } from "./src/signals/ObservableSocket";

export var tools = {
    signals: {
        SignalSocket,
        ObservableSocket,
    }
};